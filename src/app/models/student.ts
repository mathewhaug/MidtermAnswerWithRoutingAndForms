//Create a model of what the student should be
export interface Student {
  id: number;
  studentName: string;
  courseCode: string;
  dateOfBirth: string; //mm/dd/yyyy
  letterGrade: string;
  profilePic: string; //URL
}
